import { View, Text } from "react-native";

const ErrorScreen = () => {
  return (
    <View style={{ flex: 1 }}>
      <Text style={{ color: "white" }}>ErrorScreen</Text>
    </View>
  );
};

export default ErrorScreen;
